/* ============================================================
   Configuração de segurança — portfólio
   Substitua turnstileSiteKey pela chave do painel Cloudflare:
   https://dash.cloudflare.com/?to=/:account/turnstile
   ============================================================ */
window.PORTFOLIO_SECURITY = {
  /* Chave pública (site key). A secret key fica apenas no servidor. */
  turnstileSiteKey: '0x4AAAAAADg4kerCdJgb1zV7',
};
